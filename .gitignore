# Dependencies
node_modules/

# Build
dist/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# NOTE: drizzle/ (migrations + meta/) MUST stay tracked — it is now the schema
# source of truth. The old `src/db/*.sql` ignore rule was removed along with the
# introspect-era snapshot it hid; `out` is ./drizzle now.

# graphify build cache (content-hashed, regenerates locally, not worth committing)
graphify-out/cache/
# graphify intermediate/machine-local files - deleted by Step 9 cleanup or per-machine, not build output
graphify-out/.graphify_python
graphify-out/.graphify_analysis.json
graphify-out/[0-9][0-9][0-9][0-9]-*/
graphify-out/.graphify_root
